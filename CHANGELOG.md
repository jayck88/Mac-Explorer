# 更新日志

## [Unreleased] - 2026-08-27

### 新增

- 新增全局排版令牌与排版服务，支持标准、紧凑、舒适三套密度预设；标题栏、侧边栏、文件列表、信息面板、对话框及设置页都统一从语义化字体令牌取值。
- 新增可持久化的交互颜色令牌服务。浅色和深色主题分别维护悬停、按下、选中、选中悬停和文本高亮颜色，并在设置页提供调色盘、十六进制输入和按主题恢复默认值的能力。
- 新增压缩包解压目录计算工具及测试。除“解压到当前文件夹”外，支持解压到以压缩包命名的独立目录；目标目录冲突时自动生成安全的唯一名称。
- 为 APK 文件增加 Android 风格文件图标。
- 为 macOS 上下文菜单新增默认应用图标读取能力；“打开”“打开方式”及 Finder 项可显示对应应用图标。

### 改进

- 将 Fluent 控件的按钮、组合框、输入框、文本选择和焦点边框接入全局交互令牌，设置页调整颜色后会同步影响同类控件。
- 重构主题设置页的交互颜色区域：按主题编辑、色板选择、颜色预览与输入校验均使用统一状态，焦点色与选中色合并为同一个语义令牌。
- 统一弹出组合框、滚动条、任务面板、信息面板、首页、AI 页面和对话框的主题资源与文字样式，使浅色、深色主题表现一致。
- 优化主窗口结构：导航/面包屑与常驻搜索区域独立布局，窗口标题可反映当前位置；侧边栏占满窗口高度。
- 优化侧边栏收藏集编辑体验：新增、重命名可直接在行内完成，悬停时再显示操作入口，并补齐 Enter、Escape 与焦点处理。

### 修复

- 移除按钮、组合框、输入框、搜索框及标题栏导航背景/边框画刷的过渡动画，修复仅悬停后移出鼠标时短暂闪现灰色背景的问题。
- 修复输入框、信息面板文本框以及组合框的高亮、文本选择和键盘焦点颜色未跟随全局令牌的问题。
- 去除设置页卡片的悬停背景效果，避免设置列表出现不必要的交互反馈。
- 修复信息面板标签按钮在指针状态下可能丢失自身颜色的问题。

### 测试与验证

- 新增并扩展压缩包解压路径、组合框样式、文件图标、侧边栏交互、交互颜色令牌、macOS 应用图标、滚动条和排版令牌的自动化测试。
- 已通过 `dotnet build MacExplorer.csproj --no-restore`、`dotnet test Tests/MacExplorer.Tests/MacExplorer.Tests.csproj --no-restore`（101/101）及 `git diff --check` 验证。
